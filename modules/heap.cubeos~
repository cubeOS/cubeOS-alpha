;;;;;;;;;;;;;;;;;;;;;;;;;;;;
; HEAPMAN ; Heap Manager   ;
;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;:# Heap Manager
:heap.loc DAT 0


;:## heap.init
;:(location, length)
;:heap.init reserves a 16 word header...

:heap.init ; (location, length)
SET C, A
ADD C, 16 ;16 word header

SET [A], 0x9EA9
ADD A, 1
SET [A], C

 ;
 ;;
